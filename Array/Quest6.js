/* write a program to print the first non-repeated number in an array. */
let arr = [
  10, 15, 17, 18, 25, 27, 30, 32, 41, 100, 15, 10, 17, 18, 25, 27, 30, 440,
];

/* function nonRep(arr) {
  let temp = 0;
  for (ele of arr) {
    if (arr.indexOf(ele) === arr.lastIndexOf(ele)) {
      temp = ele;
      break;
    }
  }
  return temp;
}
console.log(nonRep(arr)); */

